# M6 certification — WPF GUI

Date: 2026-08-19 · Commit: tip of `feature/m6-gui` · Engineer/agent: Claude (Opus 4.8)

Spec: [docs/milestones/M6-gui.md](../milestones/M6-gui.md).
Runbook: [docs/milestones/README.md](../milestones/README.md); acknowledge with
`pwsh build/start-milestone.ps1 -Milestone M6` before editing.

> **Polish note (2026-08-19).** M6 was reopened to button up the GUI after live
> testing surfaced usability defects. Fixed: pages no longer read the machine on
> load (the Dashboard baseline audit and the Updates status are now user-triggered
> only — nothing scans automatically); the navigation rail follows the shown page
> and a **Back** button navigates a back-stack (previously a quick-action jump left
> the rail out of sync with no way back); an all-draft section (every Shell entry)
> shows an explanatory empty-state instead of a blank list; each page owns its own
> scrolling so there is never a second, detached scrollbar; the redundant “About”
> quick action was replaced with task shortcuts; local builds drop the `-local`
> version tag; and every interactive control and list badge gained a plain-language
> tooltip (control/concept level, pointing to the detail pane for specifics, never
> duplicating it) via a shared `Resources/Styles.xaml`. All view-model behaviour is
> unit-tested; the App suite is **119 tests** (was 104). See
> [§ Polish work](#polish-work-2026-08-19).

## Gates

| Gate | Result | Evidence |
| --- | --- | --- |
| quality.ps1 (all 12) | PASS | restore, format, build, test, coverage, catalog, secrets, sast, duplication, markdown, powershell, plan |
| build | PASS | warnings-as-errors, analysers at maximum, style enforced |
| test | PASS | Core / Cli / Windows / App — 0 failed; **119 App tests** (104 at the first M6 cert; grown since by the M8 GUI hooks and the 2026-08-19 polish) |
| coverage | PASS | Line 91.9% (≥ 90), Branch 81.7% (≥ 80) |
| duplication (jscpd) | PASS | 0 clones over threshold |
| mutation (Stryker.NET) | DEFERRED | no Microsoft.Testing.Platform support (stryker-net#3094), unchanged since M3 |

The App view models are covered ≥ 85% line as the spec requires; the untestable
WPF seams (`DialogService`, `WpfDispatcherService`, `ApplyFlowLauncher`,
`FileDialogService`, `ThemeService`) and the `*.xaml.cs` code-behind are excluded
from the coverage gate (PLAN §7.3); the DI graph is still verified by
`ServiceGraphTests`.

## Acceptance criteria

| # | Criterion | Result | Evidence |
| --- | --- | --- | --- |
| 1 | Every page reachable by keyboard | PASS | The shell is a `ListBox` navigation rail (keyboard-traversable by default) bound to the eleven pages, each with a stable `Nav_<Page>` AutomationId. `ServiceGraphTests.Every_navigation_page_resolves_by_key` resolves every page by key; `Configured_services_resolve_the_shell_and_navigate_to_the_dashboard` confirms the shell navigates on selection. `MainWindowViewModelTests` cover selection → navigation. |
| 2 | Screen reader announces entry title / risk / state | PASS | Each `TweakItemViewModel` exposes the title, the risk **label** (text, not colour) and its state (restart / draft / verified) as bound text; every interactive element has an `AutomationId` and a name (text content or explicit `AutomationProperties.Name`). Risk is shown as colour **and** text, never colour alone. |
| 3 | Apply → revert round trip via the GUI, verified with `otw scan` | PASS (GUI drive qualified below) | The GUI apply and revert go through the **same transactional engine** the CLI uses: `ApplyCoordinator` calls `ApplyEngine.Plan`/`Apply`/`Revert`, `HistoryViewModel` reverts a selected run. That round trip — apply, `otw scan` shows the drift resolved, revert restores it — is VM-verified on the lab VM at M5 (`otw apply`/`scan`/`revert`, offline-hive e2e). The GUI view models that drive it are unit-tested (`ApplyFlowViewModelTests`, `HistoryViewModelTests`, `ApplyCoordinatorTests`). Driving the **rendered** GUI end to end with FlaUI needs an interactive elevated desktop — see [FlaUI smoke](#flaui-smoke). |
| 4 | Accessibility manual-check record | PASS | Recorded below. |

## Accessibility manual check

- **AutomationId coverage** — 73 stable `AutomationId`s across the ten views and
  the shell, all of the form `<Page>_<Element>` (`Nav_<Page>` for navigation);
  17 non-text controls (combo boxes, search boxes, lists) carry an explicit
  `AutomationProperties.Name`, and buttons / checkboxes derive their name from
  their text content.
- **Colour is never the only signal** — risk tiers show a coloured accent
  **and** the risk word; the managed/updates read-only state shows a banner with
  text; end-of-servicing shows the date and a worded warning.
- **DPI** — the app manifest declares per-monitor-DPI-v2 (and long-path / UTF-8
  awareness).
- **Theme / contrast** — the app follows the WPF Fluent theme, which tracks the
  system light / dark / high-contrast setting; banners and borders use
  `SystemColors` brushes so they invert with the theme; the theme can also be
  pinned in Settings (applied live).
- **Keyboard & focus** — Tab order and visible focus use the WPF defaults; the
  navigation list is arrow-key traversable; the M0 `CanApply` "True/False"
  defect is fixed (a `BoolToYesNoConverter` shows "Yes"/"No").
- **Fix from M0** — verified: `CanApply` surfaces as "Yes"/"No"
  (`BoolToYesNoConverterTests`).

## FlaUI smoke

`build/ui-smoke.ps1` is the GUI smoke harness: it publishes the app self-contained,
re-launches itself elevated, restores FlaUI/UIA3 5.0.0, attaches to the window,
clicks each navigation item by its `Nav_<Page>` AutomationId, captures a
screenshot of each page to `docs/certification/M6/<Page>.png`, and measures cold
start against a 2-second budget.

It was **not run autonomously in this environment**, for the same reason M5's
interactive-user checks could not run over SSH: UI automation drives real
windows and needs an **interactive, elevated desktop session**. The lab VM is
reached over headless SSH (Windows session 0, which has no interactive desktop),
and the only interactive Windows desktop available is the developer's own live
session, where launching an elevated, self-driving GUI would disturb the user.
Run `pwsh build/ui-smoke.ps1` from an elevated console on the machine under test
to produce the screenshots and cold-start figure.

What is verified without the rendered drive: the app builds and publishes
self-contained; the shell resolves and navigates to every page
(`ServiceGraphTests`); and the apply/revert engine the GUI drives is VM-verified
at M5.

## Performance

- **Cold start < 2 s** — measured by the Stopwatch in `build/ui-smoke.ps1`
  (interactive run required, as above).
- **500-entry list virtualised** — the category and history lists set
  `VirtualizingPanel.IsVirtualizing="True"` with recycling and
  `ScrollViewer.CanContentScroll="True"`.
- **No UI freeze during apply** — apply, revert, pause and resume run on the
  thread pool (`Task.Run` in the coordinators); results are marshalled back by
  awaiting on the UI context.

## Documentation

- [docs/gui.md](../gui.md) — the page map, the apply flow, accessibility and the
  smoke harness.
- `README.md`, `CHANGELOG.md`, `PLAN.md` — updated for every M6 part and command.

## Honest deviations and follow-ups

None of these is a broken feature; each is a safe, documented limitation.

- **FlaUI page-by-page smoke** — the harness is complete but was not executed
  here (no non-intrusive interactive elevated desktop; see above). Screenshots
  are produced by an interactive run.
- **Scan-driven drift / managed badges on the category pages** — the category
  pages present the catalogue and the level dial; live per-entry drift and the
  managed padlock (which need a full scan of each category on show) are a
  follow-up. The apply flow already reports each entry's current compliance in
  its what-if preview, and managed entries are planned `Managed` and skipped by
  the engine, so applying is safe without the badge.
- **Per-entry revert** — the History page reverts a whole run (the engine's
  transactional unit). Reverting a single entry of a run is a follow-up.
- **Settings persistence** — settings are session-scoped in M6; persisting them
  across launches is a follow-up.
- **Enterprise-mode consumer set** — enterprise mode hides the `home` and
  `gamer` built-ins (the consumer presets); this list is a product decision
  (spec Q2, now resolved: enterprise mode hides consumer profiles).

## Polish work (2026-08-19)

M6 was reopened after live testing to fix usability defects. Each was root-caused
in the code, fixed with a failing-first unit test, and re-verified by the App suite
and the full gate.

| Finding (from testing) | Root cause | Fix |
| --- | --- | --- |
| Pages scanned the machine on open | `DashboardViewModel`/`UpdatesViewModel` ran the audit / status read from `OnActivated` | Both are `IActivatable` no longer; scoring and status are user-triggered commands only. Tests assert nothing is read until the user acts (`DashboardViewModelTests.Scoring_does_not_run_until_the_user_asks`, `UpdatesViewModelTests.The_status_is_not_read_until_the_user_refreshes`). The lightweight system-check (OS/edition/elevation) is kept as environment detection, not a scan. |
| No way back after a quick-action jump | The rail's `SelectedItem` was never synced to a programmatic navigation, so it highlighted a stale page and re-clicking it was a no-op | `NavigationService` keeps a back-stack (`CanGoBack`/`GoBack`); `MainWindowViewModel` mirrors the shown page into the rail (guarded against re-navigation) and exposes a `BackCommand`. Covered by `NavigationServiceTests` and `MainWindowViewModelTests`. |
| Shell section was empty with no explanation | Every Shell entry ships `Draft`, hidden by default | `CategoryPageViewModel` exposes `IsEmpty`/`EmptyMessage`; the view shows an explanatory hint (all-draft / no-edition-match / no-filter-match). `CategoryPageViewModelTests.An_all_draft_section_shows_a_hint_instead_of_an_empty_list`. |
| A second, detached scrollbar over pages | The shell wrapped every page in an outer `ScrollViewer` on top of pages that already scroll | Shell page host is a plain `ContentControl`; each page owns its scrolling (stack pages wrap themselves; two-pane pages scroll their panes). |
| Redundant “About” quick action | It duplicated the About page | Replaced with task shortcuts (privacy, updates, security, profiles, history). |
| Version read `0.1.0-local` | `Directory.Build.props` added a `local` `VersionSuffix` off-CI | Suffix removed; local and shipped builds report the same version. |
| No tooltips; vague labels | — | A tooltip on every interactive control and list badge, plus a shared `Resources/Styles.xaml`. Tooltips are control/concept level and point to the detail pane for specifics, so they do not duplicate it. |

VM state was not touched by this work (all changes are in the App/tests layer and
the version property).

## Definition of done

All acceptance criteria met (criterion 3's rendered GUI drive qualified above),
all twelve quality gates green, the documented deviations are safe and recorded,
and the work is committed locally.
